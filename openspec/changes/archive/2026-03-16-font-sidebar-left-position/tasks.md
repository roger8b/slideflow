## 1. Corrigir Posicionamento do FontSidebar

- [x] 1.1 Em `FontSidebar.tsx`, mudar `right-0` para `left-0` na className do `motion.div`
- [x] 1.2 Mudar `initial={{ x: 320 }}` e `exit={{ x: 320 }}` para `x: -320` na animação do `motion.div`
- [x] 1.3 Mudar `border-l` para `border-r` na className do `motion.div`
